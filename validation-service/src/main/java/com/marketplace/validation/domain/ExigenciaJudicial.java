package com.marketplace.validation.domain;

/**
 * Resultado de la validación judicial (mock). {@link #NO_REQUERIDO} habilita aprobación automática si el resto es ALTA.
 */
public enum ExigenciaJudicial {
    NO_REQUERIDO,
    REQUERIDO
}
