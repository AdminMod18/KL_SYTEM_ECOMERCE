# ALB/ECS: GET /validation/actuator/health
management:
  endpoints:
    web:
      base-path: /validation/actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
