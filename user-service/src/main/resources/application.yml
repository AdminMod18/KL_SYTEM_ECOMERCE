server:
  port: ${SERVER_PORT:9002}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# Secreto compartido con auth-service para POST /interno/verificar-credenciales
integration:
  interno:
    secret: ${USER_SERVICE_INTERNAL_SECRET:kl-dev-internal-secret-cambiar}

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:h2:mem:users;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
