# ALB/ECS: GET /payments/actuator/health
management:
  endpoints:
    web:
      base-path: /payments/actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
