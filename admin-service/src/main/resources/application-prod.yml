# Perfil prod (ECS): PostgreSQL RDS. Variables DB_* o SPRING_DATASOURCE_* desde la task definition.
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:kl_ecommerce}
    username: ${DB_USER:${SPRING_DATASOURCE_USERNAME:kl_admin}}
    password: ${DB_PASSWORD:${SPRING_DATASOURCE_PASSWORD:}}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: false

# ALB/ECS esperan GET /admin/actuator/health (Terraform prod).
management:
  endpoints:
    web:
      base-path: /admin/actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
