package com.marketplace.analytics.config;

import org.springframework.context.annotation.Configuration;

/**
 * Propósito: módulo de configuración del microservicio de analítica.
 * Patrón: configuración Spring (punto de extensión).
 * Responsabilidad: agrupar beans de configuración futuros (serialización, seguridad, etc.).
 */
@Configuration
public class AnalyticsConfig {
}
